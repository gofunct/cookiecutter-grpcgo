protoc:
  version: 3.6.1

generate:
  go_options:
    import_path: github.com/{{cookiecutter.github_username}}/{{cookiecutter.app_name}}
  plugins:
  - name: gogoslick
    type: gogo
    flags: plugins=grpc
    output: .
