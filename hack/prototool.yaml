protoc:
  version: 3.6.1

generate:
  go_options:
    import_path: github.com/gofunct/user
  plugins:
  - name: gogoslick
    type: gogo
    flags: plugins=grpc
    output: .
  - name: gotemplate
    flags: template_dir=templates
    output: .
  - name: cobra
    flags: plugins=client
    output: .